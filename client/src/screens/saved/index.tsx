import Container from '../../components/container'

export default function Saved() {
  // useQuery on me query

  return (
    <Container style={{ paddingTop: 40, paddingBottom: 40 }}>
      My saved pokemon
    </Container>
  )
}
